import type { JSONSchemaType } from 'ajv';
/**
 * Seems the secure schema is not strict, this causes warnings to be logged
 *
 * @internal */
export declare const validateSchemaSecure: import("ajv").ValidateFunction<unknown>;
export declare function isSecureSchema<T = Record<string, any>>(schema: JSONSchemaType<T>): boolean;
