import type { GraphQLModel, ItemField } from '@glazed/graphql-types';
import type { Schema } from '@glazed/types';
import type { ModelManager } from './datamodel';
/** @internal */
export declare function getItemField(model: GraphQLModel, schema: Schema, parent: string, owner: string): ItemField;
export declare type AddModelSchemaOptions = {
    name?: string;
    parent?: string;
    owner?: string;
};
/**
 * Add a JSON schema to the provided records based on its type
 *
 * @internal
 * */
export declare function addModelSchema(model: GraphQLModel, schema: Schema, options?: AddModelSchemaOptions): string;
export declare function createGraphQLModel(manager: ModelManager): Promise<GraphQLModel>;
