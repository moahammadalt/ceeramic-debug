import type { Schema } from '@glazed/types';
/** @internal */
export declare function getReference(schema: Schema): Array<string> | null;
/**
 * Recursively extract references to other schemas from a JSON schema arrays and objects
 *
 * @internal */
export declare function extractSchemaReferences(schema: Schema, path?: string): Record<string, Array<string>>;
